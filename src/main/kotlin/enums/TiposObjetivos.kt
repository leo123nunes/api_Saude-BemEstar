package enums

// Este enum representa o tipo de objetivo que o usuário tem, por padrão todos os
// usuários são cadastrados incialmente com o objetivo normal

enum class TiposObjetivos {
    NORMAL, HIPERTROFIA, FITNESS
}